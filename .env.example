OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Plaid (Option A aggregator) — required for Connect page
PLAID_CLIENT_ID=
PLAID_SECRET=
# sandbox | development | production
PLAID_ENV=sandbox

# Optional: require sign-in (both must be set to enable the gate)
VAULTBOARD_USERNAME=
VAULTBOARD_PASSWORD=
